import 'package:flutter/material.dart';

class MyTheme {
  // Base colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFFB0B0B0);
  static const Color darkGrey = Color(0xFF707070);
  static const Color green = Color(0xFF4CAF50);

  // Primary and secondary colors
  static const Color primaryColor = green;
  static const Color secondaryColor = darkGrey;

  // Background and surface colors
  static const Color backgroundColor = white;
  static const Color surfaceColor = white;

  // Text and icon colors
  static const Color textColor = black;
  static const Color secondaryTextColor = darkGrey;
  static const Color iconColor = black;

  // Divider and shadow colors
  static const Color dividerColor = grey;
  static const Color shadowColor = darkGrey;

  // Status colors
  static const Color errorColor = Color(0xFFFF0000);
  static const Color successColor = green;
  static const Color warningColor = Color(0xFFFFA500);
  static const Color infoColor = Color(0xFF2196F3);

  // Link color
  static const Color linkColor = Color(0xFF1E88E5);
}
